enum UserRole {
  admin,
  user,
}

enum ContentType {
  mockTest,
  pdf,
  note,
  video,
  practiceSet,
}

enum AccessType {
  free,
  paid,
}

enum PaymentStatus {
  pending,
  success,
  failed,
  refunded,
}

enum AdPlacement {
  homeTop,
  homeMiddle,
  examList,
}

enum TestSessionStatus {
  ongoing,
  completed,
  abandoned,
}

enum AppThemeType {
  primaryBlue,
  primaryGreen,
  primaryRed,
}
